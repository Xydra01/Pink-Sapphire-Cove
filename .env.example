# Dragon Cave: application "Private Key" from https://dragcave.net/api/manage
# (not the OAuth client id). Paste the key alone; do not add "Bearer " for .env.
# Used for v2 Bearer and legacy /api/{key}/json/... (both accept this key).
DC_API_KEY=
MONGODB_URI=
MONGODB_DB=
